/*
  tested on:
  - desktop jar + x86_64-linux + x
  - desktop jar + x86_64-linux + xvfb
*/

if (Vars.headless)
  throw "Pulverizer does not support headless clients. Check the README for a workaround.";
